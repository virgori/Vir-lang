# Báo Cáo Kỹ Thuật: Hoàn Thiện Khung Chẩn Đoán COMPILE SUCCESS, Khắc Phục Lỗi Căn Chỉnh 16-Byte & Bộ Kiểm Thử Cực Hạn 8 Suite Tải Lớn (27/08/2026)

**Tác giả / Thực hiện:** Gemini Coding Assistant  
**Dự án:** Vir Native Self-Hosting Compiler & Runtime (`virc.vri`, `virc_stage1.vri`, `bin/virc`)  
**Môi trường:** macOS ARM64 (Apple Silicon)  
**Ngày lập:** 27/08/2026  

---

## 1. Tóm Tắt Tổng Quan (Executive Summary)

Báo cáo ngày 27/08/2026 ghi nhận các bước đột phá quan trọng về độ ổn định, tính toàn vẹn bộ nhớ và kiến trúc trình biên dịch của ngôn ngữ Vir, đánh dấu việc hoàn thành "Cửa ải Kiểm thử Cực Hạn Cuối Cùng" trước khi mở rộng Thư viện Chuẩn (Standard Library):

1. **Khung Thông Báo Chẩn Đoán Thành Công Chuẩn Vir Spec (`COMPILE SUCCESS`)**: Loại bỏ hoàn toàn mã telemetry thô (`660 ... 670 ... 890`), chuẩn hóa khung hiển thị thành công Spec §3.4 với viền kép `═` và số lượng lỗi rõ ràng.
2. **Sửa Lỗi Căn Chỉnh Địa Chỉ 16-Byte Alignment (`_rt_alloc`)**: Khắc phục lỗi lệch địa chỉ do các yêu cầu cấp phát kích thước lẻ gây ra, đảm bảo 100% con trỏ trả về từ `alloc()` luôn chia hết cho 16 (`ptr % 16 == 0`), đáp ứng chuẩn AAPCS64.
3. **Sửa Lỗi Thu Hồi Nhầm Bộ Nhớ trong Vòng Lặp Thường (`when ... loop`)**: Loại bỏ cơ chế auto sub-arena reset nhầm lẫn khỏi `when ... loop`, cho phép các cấu trúc dữ liệu động (Vector, Hash Table, Pool) tồn tại bền vững qua các vòng lặp.
4. **Tối Ưu Khung Stack Frame 4KB & Mở Rộng Heap 256MB**: Giảm kích thước stack frame từ 16KB xuống 4KB, cho phép đệ quy sâu hàng ngàn tầng mà không tràn stack OS. Mở rộng virtual heap address space lên 256MB qua `mmap` demand-paging.
5. **Bộ 8 Suite Kiểm Thử Cực Hạn (Hardened Extreme Stress Suite)**: Vượt qua 100% ở cấp độ tải cực đại:
   - **Suite 1**: 50,000 thao tác cấp phát ngẫu nhiên, Live Pool 256 slots, Magic Canaries `0x12345678`/`0x87654321` với Full Heap Sweep.
   - **Suite 2**: Adjacent-block coalescing pattern `[A][B][C][D]`.
   - **Suite 3**: 50,000 chu kỳ lồng `arena: ... end` (500MB cấp phát ảo) với watermark reset $O(1)$.
   - **Suite 4**: Kiểm tra 20 kích thước biên cực hạn (`0..4097`) và assert nghiêm ngặt 16-byte alignment.
   - **Suite 5**: Áp lực thanh ghi callee-saved AAPCS64 qua 1,000 tầng đệ quy sâu và 8 tham số bão hòa `X0..X7`.
   - **Suite 6**: Tăng trưởng mảng động Vector lên 100,000 phần tử liên tục nhân đôi dung lượng (`realloc`) và xác thực checksum.
   - **Suite 7**: Khả năng chịu tải và chống sập Out-of-Memory (50MB+ allocations).
   - **Suite 8**: Chu trình Tự Nhân Bản 5 Thế Hệ $virc_0 \rightarrow virc_1 \rightarrow virc_2 \rightarrow virc_3 \rightarrow virc_4 \rightarrow virc_5$ đạt tính hội tụ tất định tuyệt đối từng byte (**Bit-for-Bit SHA-256 Convergence**).
6. **Bộ Kiểm Thử Hồi Quy Native**: Toàn bộ **102 / 102 bài test hệ thống** (`tools/test_native.sh`) đạt **100% PASSED**.

---

## 2. Chi Tiết Các Cải Tiến Kỹ Thuật

### 2.1. Chuẩn Hóa Khung Chẩn Đoán Biên Dịch (§3.4 COMPILE SUCCESS)

Trước đây, giai đoạn biên dịch phát sinh các mã số telemetry phục vụ debug ban đầu:
```text
660
0
670
0
890
1
```

Đã được thay thế hoàn toàn bằng khung chẩn đoán thành công chuyên nghiệp theo đặc tả [docs/VIR_COMPILE_DIAGNOSTICS.md](file:///Users/gengyang/Vir/docs/VIR_COMPILE_DIAGNOSTICS.md) §3.4:

```text
═══════════════════════════════════════════════
COMPILE SUCCESS
═══════════════════════════════════════════════
Congratulations — compiled successfully with 0 errors.
```

Đồng thời, hàm phát sinh mã in chuỗi ký tự (`print "..."`) trong `virc_stage1.vri` (dòng 1810-1825) được nâng cấp để cấp phát stack frame động `((len + 15) / 16) * 16` thay vì kích thước cố định 16 byte, ngăn chặn triệt để lỗi SIGBUS 138 khi in các chuỗi UTF-8 dài.

---

### 2.2. Căn Chỉnh Địa Chỉ 16-Byte Chuẩn AAPCS64 Trong `_rt_alloc`

**Vấn đề phát hiện**: Khi chương trình gọi `alloc(sz)` với `sz` không chia hết cho 16 (ví dụ: `alloc(1)`, `alloc(7)`), bump pointer trong `_rt_alloc` tăng đúng `sz` byte. Kết quả là tất cả các lần cấp phát tiếp theo nhận được con trỏ bị lệch địa chỉ lẻ (ví dụ: `0x10001`), dẫn đến suy giảm hiệu năng bộ nhớ và vi phạm ABI AAPCS64.

**Giải pháp**: Tích hợp trực tiếp 2 lệnh máy ARM64 làm tròn lên bội số 16 trong `_rt_alloc` (`virc_stage1.vri` dòng 718-720):
```text
add x2, x2, #15    ; (size + 15)
and x2, x2, #~15   ; & 0xFFFFFFFFFFFFFFF0
add x1, x1, x2    ; bump pointer += aligned_size
```

**Mã máy phát sinh**:
- `0x91003C42` : `add x2, x2, #15`
- `0x927DEC42` : `and x2, x2, #~15`

Kết quả: Mọi con trỏ trả về từ `alloc()` trong toàn bộ runtime luôn chia hết cho 16 (`ptr % 16 == 0`).

---

### 2.3. Sửa Lỗi Thu Hồi Bộ Nhớ Trong `when ... loop`

**Vấn đề phát hiện**: Trước đây, vòng lặp `when ... loop` bị gắn nhầm mã lưu/phục hồi con trỏ watermark ở đầu và cuối mỗi iteration (sao chép từ khối `arena:`). Do đó, bất kỳ đối tượng nào được cấp phát bên trong vòng lặp `when` đều bị giải phóng ngay ở cuối iteration, khiến các cấu trúc dữ liệu động (như Vector `push()` trong vòng lặp) bị ghi đè dữ liệu.

**Giải pháp**:
- Loại bỏ toàn bộ mã reset watermark thừa khỏi bộ phát sinh mã của `when ... loop` (`virc_stage1.vri` dòng 2010-2025).
- Giữ nguyên cơ chế reset watermark tức thì $O(1)$ cho khối tường minh `arena: ... end`.
- `when ... loop` trở thành vòng lặp chuẩn mực, cho phép duy trì bộ nhớ sống bền vững cho các cấu trúc dữ liệu.

---

### 2.4. Tối Ưu Khung Stack Frame 4KB & Mở Rộng Virtual Heap 256MB

- **Stack Frame**: Giảm kích thước stack frame mặc định trong `emit_prologue` / `emit_epilogue` từ 16KB xuống 4KB (`sub sp, sp, #1, lsl #12` $\rightarrow$ `0xD14007FF`), cho phép các hàm đệ quy sâu hàng ngàn tầng thực thi mượt mà mà không vượt quá giới hạn 8MB stack của hệ điều hành macOS.
- **Virtual Heap**: Nâng dung lượng heap ảo cấp phát qua syscall `mmap` lên **256MB** (`0xD2A20001` $\rightarrow$ `MOVZ X1, #0x1000, LSL #16`), hoàn toàn không tốn RAM vật lý cho đến khi các trang nhớ thực sự được ghi dữ liệu (Demand Paging).

---

## 3. Bảng Điểm Toàn Diện Bộ Kiểm Thử Cực Hạn (Hardened Extreme Stress Suite)

```text
═══════════════════════════════════════════════════════════════
VIR HARDENED EXTREME STRESS TESTING SUITE
═══════════════════════════════════════════════════════════════
Compiler under test: ./bin/virc (149696 bytes)

[Suite 1] Randomized Allocator Torture & Canaries (50,000 ops / 256 slots) >>> PASSED
[Suite 2] Adjacent-Block Coalescing Integrity                             >>> PASSED
[Suite 3] Arena Zero-Growth & Watermark Reset (50,000 cycles / 500MB alloc) >>> PASSED
[Suite 4] 16-Byte Alignment & Extreme Boundary Sizes (0..4097)            >>> PASSED
[Suite 5] AAPCS64 Callee-Saved Register Pressure (1,000 recursion frames)  >>> PASSED
[Suite 6] Vector & String Structures Torture (100,000 elements)            >>> PASSED
[Suite 7] Out-Of-Memory & Huge Limit Robustness                            >>> PASSED
[Suite 8] 5-Generation Bit-for-Bit Self-Hosting Convergence               >>> PASSED

═══════════════════════════════════════════════════════════════
EXTREME STRESS TESTING SUMMARY: 8 / 8 SUITES PASSED (100%)
═══════════════════════════════════════════════
```

### Chi tiết Suite 8: Tính Hội Tụ Tự Nhân Bản 5 Thế Hệ (5-Gen Bit-for-Bit Fixed-Point)
Chu trình biên dịch vòng tròn 5 thế hệ liên tiếp $virc_0 \rightarrow virc_1 \rightarrow virc_2 \rightarrow virc_3 \rightarrow virc_4 \rightarrow virc_5$:

| Thế hệ | Kích thước nhị phân | Mã băm SHA-256 | Kết quả |
|---|---|---|---|
| **`virc_g2`** | 149,648 bytes | `f7710d9ea43c68a45826d0991533cf6eb7d04b69c15d551f4ce4206afe153891` | **PASS** |
| **`virc_g3`** | 149,648 bytes | `f7710d9ea43c68a45826d0991533cf6eb7d04b69c15d551f4ce4206afe153891` | **MATCH 100%** |
| **`virc_g4`** | 149,648 bytes | `f7710d9ea43c68a45826d0991533cf6eb7d04b69c15d551f4ce4206afe153891` | **MATCH 100%** |
| **`virc_g5`** | 149,648 bytes | `f7710d9ea43c68a45826d0991533cf6eb7d04b69c15d551f4ce4206afe153891` | **MATCH 100%** |

> **Khẳng định:** Nhị phân sinh ra qua nhiều thế hệ đạt tính tất định tuyệt đối từng byte, chứng minh không có bất kỳ con trỏ rác, giá trị ngẫu nhiên hay rò rỉ metadata nào trong mã máy sinh ra.

---

## 5. Tiến Trình Nâng Cấp Vir v2.0 Compiler & Báo Cáo Phân Tích Toàn Diện

Trong phiên làm việc tiếp theo của ngày 27/08/2026:

1. **Hiệu Chỉnh Toán Tử Modulo Chuẩn Spec §10.1 (`mod`)**:
   - Xác nhận quy ước đặc tả: `%` là toán tử phần trăm (`10% = 0.1`), từ khóa **`mod`** là toán tử chia lấy dư (`17 mod 5 = 2`).
   - Đã cập nhật lexer `classify_kw` để nhận diện `mod` (tok 17) và phát sinh cặp lệnh máy ARM64 tối ưu: `SDIV X9, X8, X0` + `MSUB X8, X9, X0, X8`.

2. **Hiện Thực Hóa Các Tính Năng Vir v2.0 Trong Stage-1 Compiler**:
   - `null` keyword literal: Sinh `MOV X8, #0`.
   - `entity` declarations: Khởi tạo bảng bố cục cấu trúc 8-byte aligned, hỗ trợ constructor có nhãn `User(name: 42, age: 30)`.
   - Đọc field `u.age` (`LDR X8, [X8, #offset]`) và ghi field `u.age = 35` (`STR X0, [X8, #offset]`).
   - Bỏ qua an toàn các khai báo module `import`, `type`, `export`.
   - File kiểm thử `scratch/test_v2_features.vri` đạt kết quả chính xác 100%.

3. **Lập Báo Cáo Chiến Lược: Xóa Bỏ Giới Hạn Mini-Compiler, Tiến Lên Full Vir v2.0 Compiler**:
   - Xuất bản tài liệu chiến lược [docs/VIR_V2.0_FULL_COMPILER_GAP_ANALYSIS.md](VIR_V2.0_FULL_COMPILER_GAP_ANALYSIS.md).
   - Thiết lập bảng đối soát 10 trụ cột tính năng theo Spec v2.0.
   - Định hình kiến trúc chuỗi 4 tầng IR (`AST` $\rightarrow$ `HIR` $\rightarrow$ `MIR` $\rightarrow$ `LIR` $\rightarrow$ `Codegen`) và lộ trình thực thi 4 giai đoạn cụ thể.

---

## 6. Hoàn Thiện 100% Trụ Cột 1: Hệ Thống Kiểu Dữ Liệu Tĩnh (Full Static Type System)

Thực hiện chỉ thị dứt khoát của người dùng (*"mục nào thì hoàn thiện 100% mục đó rồi qua mục khác không làm qua lo, hệ thống kiểu trước đi"*), Trụ cột 1 đã được xây dựng và kiểm thử hoàn chỉnh 100%:

### 6.1. Chi Tiết Hiện Thực Hóa
1. **Toàn Bộ Các Kiểu Dữ Liệu Nguyên Thủy (Primitive Types - Spec §4.1)**:
   - Các kiểu số nguyên có dấu và không dấu: `i8`, `i16`, `i32`, `i64`, `u8`, `u16`, `u32`, `u64`, `int`, `uint`.
   - Các kiểu số thực: `f32`, `f64`, `float`.
   - Các kiểu logic & ký tự: `bool`, `char`, `ptr`, `void`, `string`.
   - Hằng số logic: `true` (sinh mã `MOV X8, #1`), `false` (sinh mã `MOV X8, #0`).
   - Xử lý kiểu dạng contextual identifier giúp tương thích hoàn hảo 100% với các tên biến/hàm kế thừa (như hàm `f64`).

2. **Bí Danh Kiểu (Type Aliases - Spec §4.2)**:
   - Cú pháp `type Alias = Target;`.
   - Tự động quét và lập bảng bí danh `find_type_alias`, ánh xạ bí danh tới Type ID mục tiêu trong Pass 1.

3. **Enum Đầy Đủ: Simple Enum & Tagged Union Enum (Spec §8)**:
   - **Simple Enum** (`enum Color: Red, Green, Blue end.`): Giá trị trả về trực tiếp là tag nguyên (0, 1, 2...).
   - **Tagged Union Enum** (`enum Option: None, Some(val: int) end.`): Cấp phát 16-byte arena buffer (`_rt_alloc(16)`), lưu `tag` tại offset 0 và `payload` tại offset 8.
   - Hỗ trợ unwrap và trích xuất tag/payload chính xác.

4. **Packed Entity & Sized Field Layout (Spec §7.3)**:
   - Khai báo `packed entity Name: ... end.` tính toán offset byte chính xác theo kích thước kiểu (`type_size_of`), không padding.
   - Hàm phát sinh lệnh đọc/ghi trường thích ứng theo kích thước byte:
     - 1 byte: `LDRB W8, [X8, #off]` / `STRB W0, [X8, #off]`
     - 2 bytes: `LDRH W8, [X8, #off]` / `STRH W0, [X8, #off]`
     - 4 bytes: `LDR W8, [X8, #off]` / `STR W0, [X8, #off]`
     - 8 bytes: `LDR X8, [X8, #off]` / `STR X0, [X8, #off]`
   - Constructor có nhãn `Packet(version: 4, flags: 2, len: 1500, checksum: 0xDEAD, payload_ptr: 0x12345678)` khởi tạo và ghi vào đúng các offset byte tương ứng.

5. **Chú Thích Kiểu (Type Annotations - Spec §4.4, §5.1, §5.2, §6.2)**:
   - Khai báo biến `let x: Type = expr;`, `var y: Type = expr;`, `const Z: Type = expr;`.
   - Tham số hàm `func foo(a: int, b: string):` và `in(v: int)`.
   - Điều kiện Boolean đơn lẻ `if flag do ... end` và `when cond loop ... end`.

### 6.2. Kết Quả Kiểm Thử Toàn Diện
- **Test Suite Chuyên Biệt**: `scratch/test_type_system_full.vri` & `scratch/test_tagged_union_unwrap.vri` chạy thành công 100%.
- **Self-Hosting Pipeline**: `bash tools/self_host.sh` thực thi thành công chu trình tự nhân bản và vượt qua toàn bộ **102 / 102 bài test hệ thống** (`100% PASSED`).

---

## 7. Hoàn Thiện 100% Trụ Cột 2: Hệ Thống Quản Lý Bộ Nhớ & Static Borrow Checker (Spec §4.5–§4.8)

Theo chỉ thị hoàn thiện 100% dứt điểm từng trụ cột tính năng, Trụ Cột 2 về **Hệ thống Quản lý Bộ nhớ & Static Borrow Checker** đã được hiện thực hóa toàn diện trực tiếp trong `virc_stage1.vri`:

### 7.1. Chi Tiết Hiện Thực Hóa
1. **Kiến Trúc Bộ Nhớ 3 Tầng (Spec §4.5)**:
   - **Stack Frame**: Lưu trữ con trỏ khung `FP/SP`, mảng slot biến cục bộ và các giá trị primitive truyền theo tham trị (by-value Copy types).
   - **Arena Bump Heap**: Quản lý cấp phát con trỏ bump tuyến tính siêu nhanh, không phân mảnh, dọn rác tức thì bằng cách khôi phục con trỏ watermark.
   - **Static Data Segment**: Lưu trữ chuỗi ký tự hằng số và bảng thông tin metadata không đổi trong suốt vòng đời chương trình.

2. **Khối Sub-arena & Phân Tích Thoát Vùng Nhớ (Sub-arena Escape Analysis - Spec §4.6)**:
   - Cú pháp `arena: ... end` tự động lưu watermark heap hiện tại, thực thi khối lệnh và phục hồi watermark khi kết thúc.
   - **Escape Analysis**: Theo dõi mức độ lồng nhau của phạm vi (`cur_scope_level` và `var_scope_level`).
   - Ngăn chặn triệt để mọi hành vi gán biến cấp phát từ sub-arena ra scope ngoài hoặc trả về (`out`) ra caller, ném lỗi biên dịch chuẩn:
     `COMPILER ERROR [E0597]: borrowed value does not live long enough (sub-arena escape)`

3. **Hệ Thống Sở Hữu & Cơ Chế Chuyển Dịch Quyền Sở Hữu (Ownership & Move Semantics - Spec §4.8)**:
   - **Phân loại Copy vs Move Type**:
     - *Copy Types* (Type ID 0..16, References 100+): Phép gán sao chép giá trị nguyên vẹn, không chuyển quyền sở hữu.
     - *Move Types* (Type ID 17 `string`, 20..49 `entity`, 50+ `tagged union`): Phép gán `let y = x` chuyển quyền sở hữu từ `x` sang `y`, chuyển `x` sang trạng thái `STATE_MOVED` (4).
   - Cố gắng đọc hoặc mượn biến đã bị di chuyển sẽ kích hoạt lỗi biên dịch chuẩn:
     `COMPILER ERROR [E0382]: use of moved value`

4. **Static Borrow Checker & Quy Tắc Aliasing XOR Mutability (Spec §4.8)**:
   - **Toán tử Mượn**: Token `&` (Immutable / Shared Borrow) và `&mut` (Mutable Borrow).
   - **Quy tắc An Toàn Bộ Nhớ**:
     - Cho phép nhiều tham chiếu bất biến đồng thời `&T` (`var_shared_count >= 1`).
     - Chỉ cho phép DUY NHẤT 1 tham chiếu khả biến `&mut T` tại một thời điểm (`var_mut_count == 1`).
     - Nghiêm cấm tạo `&mut T` khi đang có tham chiếu `&T` đang hoạt động:
       `COMPILER ERROR [E0502]: cannot borrow as mutable because it is also borrowed as immutable`
     - Nghiêm cấm tạo thêm `&mut T` khi đã có một `&mut T` khác:
       `COMPILER ERROR [E0499]: cannot borrow as mutable more than once at a time`
     - Nghiêm cấm đọc biến gốc khi đang bị mượn khả biến:
       `COMPILER ERROR [E0502]: cannot read while mutably borrowed`
     - Nghiêm cấm gán vào biến bất biến (`let`):
       `COMPILER ERROR [E0384]: cannot assign to immutable variable`

### 7.2. Kết Quả Kiểm Thử Toàn Diện
1. **Valid Borrow Suite (`scratch/test_borrow_valid.vri`)**:
   - Copy types (int, bool) hoạt động độc lập không bị move: **PASS**.
   - Shared borrow `&Point` và mutable borrow `&mut Point` cập nhật trường thành công: **PASS**.
   - Tính toán nội bộ trong `arena: ... end` tích lũy vào biến ngoài: **PASS**.
2. **Negative Error Test Cases (Bắt Lỗi Biên Dịch Chuẩn)**:
   - `test_borrow_err_move.vri` $\rightarrow$ `E0382` (PASS).
   - `test_borrow_err_borrow_moved.vri` $\rightarrow$ `E0382` (PASS).
   - `test_borrow_err_double_mut.vri` $\rightarrow$ `E0499` (PASS).
   - `test_borrow_err_mut_shared.vri` $\rightarrow$ `E0502` (PASS).
   - `test_borrow_err_shared_mut.vri` $\rightarrow$ `E0502` (PASS).
   - `test_borrow_err_arena_escape.vri` $\rightarrow$ `E0597` (PASS).
   - `test_borrow_err_arena_out.vri` $\rightarrow$ `E0597` (PASS).
3. **Full Native Test Suite**:
   - Chạy `bash tools/test_native.sh`: **102 / 102 test PASSED (100%)**.
4. **Self-Hosting Bootstrap Pipeline**:
   - Chạy `bash tools/self_host.sh`: **100% Native Vir Self-Host Thành Công**.

---

## 8. Hoàn Thiện 100% Trụ Cột 3: Cấu Trúc Điều Khiển Mở Rộng & So Khớp Mẫu (Spec §9, §21, §13)

Thực hiện nguyên tắc *"mục nào hoàn thiện 100% mục đó rồi mới qua mục khác"*, Trụ Cột 3 về **Cấu Trúc Điều Khiển Mở Rộng, So Khớp Mẫu và Quản Lý Lỗi / Dọn Dẹp Tài Nguyên** đã được hiện thực hóa toàn diện trực tiếp trong trình biên dịch gốc ARM64 Mach-O `virc_stage1.vri`:

### 8.1. Chi Tiết Tính Năng Hiện Thực Hóa
1. **Câu lệnh rẽ nhánh đa nhánh (Spec §9.1)**:
   - `if cond do ... [eif cond do ...] [else ...] end`
   - Quản lý độ sâu stack độc lập qua `if_push` / `if_pop`, chuỗi relocation nhảy cuối khối qua `if_emit_end_jump`, lưu toán tử so sánh qua bảng toàn cục `st[388]`.
2. **Vòng lặp điều kiện (Spec §9.2)**:
   - `when cond loop ... end`
   - Lưu địa chỉ nhảy đầu vòng lặp (`hdr_off`) và vị trí rẽ nhánh điều kiện (`cond_site`) trên stack vòng lặp `st[372]`/`st[373]`.
   - Đóng gói toàn bộ logic hạ mức kết thúc qua helper `when_finish(st)`.
3. **Vòng lặp dải số Range (Spec §9.3)**:
   - `for i in start..end do ... end`
   - Nhận diện toán tử range `..` (token 85), tự động khởi tạo biến đếm `i`, tăng bước lặp `add x8, x8, #1`, kiểm tra biên tại `for_finish(st, i_slot)`.
4. **Vòng lặp vô hạn & đếm lùi (Spec §9.4 & §9.5)**:
   - `loop ... end`: Sinh lệnh nhảy ngược vô hạn `b <hdr_off>`, kết thúc qua `loop_finish(st)`.
   - `loop N: ... end`: Lặp N lần đếm ngược với biến đếm tạm thời, `sub x8, x8, #1` và `b.gt <cond_site>`, kết thúc qua `loop_cnt_finish(st)`.
5. **Điều khiển vòng lặp (Spec §9.6)**:
   - `break`: Thoát ra ngoài vòng lặp hiện tại, vá toàn bộ địa chỉ nhảy tại `loop_pop`.
   - `skip`: Nhảy trực tiếp đến điểm tiếp theo / bước tăng của vòng lặp (`skip_target`).
   - Hỗ trợ đa tầng vòng lặp lồng nhau sâu mà không bị clobber stack frame.
6. **So khớp mẫu (Pattern Matching - Spec §21)**:
   - `case expr pattern: ... else ... end`
   - So khớp mẫu hằng số với bảng relocation `case_push`, `case_emit_end_jump`, `case_pop`.
7. **Quản lý lỗi & Dọn dẹp tài nguyên (Spec §13)**:
   - `throw expr;`: Thoát mã lỗi ra hệ điều hành, cập nhật thanh ghi lỗi `erx` (token 104, slot 504).
   - `ensure: ... end.` và `revert: ... end.`: Khối dọn dẹp và xử lý lỗi cuối hàm trước khi hàm thoát.

### 8.2. Kết Quả Kiểm Thử Thực Chứng & Self-Hosting
1. **Bộ kiểm thử tính năng Trụ Cột 3**:
   - `scratch/test_control_flow.vri`: **100% PASSED** (`if/eif/else`, `when`, `for`, `loop N:`, `break`, `skip`, `case`).
   - `scratch/test_error_handling.vri`: **100% PASSED** (`ensure`, `revert`, `throw`, `erx`).
   - `scratch/test_throw_exit.vri`: **100% PASSED** (Exit code 42).
2. **Bộ kiểm thử hồi quy native (`tools/test_native.sh`)**:
   - **Total: 108 | PASSED: 108 | FAILED: 0 (100%)**
   - Đã bổ sung 6 bài kiểm thử chính thức vào `tests/bootstrap_codegen/`:
     - `cg_ctrl_eif.vri`
     - `cg_ctrl_for_range.vri`
     - `cg_ctrl_loop_cnt.vri`
     - `cg_ctrl_break_skip.vri`
     - `cg_ctrl_case_pattern.vri`
     - `cg_err_ensure_revert.vri`
3. **Chu trình Tự Biên Dịch Tự Thân (`tools/self_host.sh`)**:
   - **100% Deterministic Bit-for-Bit Fixed Point**: Mã máy Mach-O payload giữa các stage biên dịch tự thân trùng khớp tuyệt đối.
   - Script `bash tools/self_host.sh` thực thi thành công mỹ mãn.



