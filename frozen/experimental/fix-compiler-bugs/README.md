# Vir freeze: experimental / fix-compiler-bugs

Filesystem pin of stdlib + compiler sources for **experimental**.

- Use this tree for release packaging or isolated experiments.
- Do **not** edit in place for ongoing development — work in the live repo, then freeze again.
- Verify integrity: `bash tools/freeze_std_tree.sh verify frozen/experimental/fix-compiler-bugs`

Created by `tools/freeze_std_tree.sh`.
