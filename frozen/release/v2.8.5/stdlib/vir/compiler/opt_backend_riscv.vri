##
 * vir/compiler/opt_backend_riscv.vri — RISC-V-only MIR/LIR optimizer hooks
 * ========================================================================
 * Independent backend passes for RISC-V 64 targets.
 ##

module vir.compiler.opt_backend_riscv

include types

func opt_riscv_run_mir_passes(blocks: i64):
    out blocks
end.

func opt_riscv_run_lir_pre(lf: i64):
    out lf
end.

func opt_riscv_run_lir_post(lf: i64):
    out lf
end.

export opt_riscv_run_mir_passes, opt_riscv_run_lir_pre, opt_riscv_run_lir_post

