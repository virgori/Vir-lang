# This directory holds platform-specific native libraries bundled into the wheel.
# Files here are populated by scripts/build_release.py before wheel packaging.
# Do not commit binary files — they are build artifacts.
*
!.gitignore
