# Example: If CPU idle, add A and B with register
# ===================================================
# Spec §5 example – End-to-end

func compute_sum A B
  if cpu_idle
    add A B register
  else
    add A B
  return A

# Example 2: Loop
function count n
  variable i 0
  loop n
    print i
  return i
