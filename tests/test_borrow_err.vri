# §4.8 Borrow checker — &mut conflict with & MUST fail compile
func take2:
    in(p, q)
    out p + q
end.
func main:
    var a = [1, 2, 3]
    var r = take2(&a, &mut a)    # ERROR: shared + mutable borrow of 'a'
    print r
end.
