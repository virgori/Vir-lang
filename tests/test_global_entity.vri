var total = 0

entity Config:
    verbose: int
end.
func process(cfg: Config, x: int) -> int:
    if cfg.verbose == 1: do
        print x
    end
    total = total + x
    out x
end.
func main:
    var cfg = Config( verbose: 1 )
    process(cfg, 10)
    process(cfg, 20)
    print total
end.