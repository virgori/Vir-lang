func foo:
    in(p: i64)
    var (p2, count) = bar(p)
    var (p4, _, _) = baz(p2)
    out (p4, count)
end.

func bar:
    in(x: i64)
    out (x, 42)
end.

func baz:
    in(x: i64)
    out (x, 0, 0)
end.

func main:
    var (a, b) = foo(1)
    print a
end.
