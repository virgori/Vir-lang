# when body auto sub-arena: alloc each iteration reclaims bump (no `arena:`).
func main:
    let i = 0
    when i < 50 loop
        let p = alloc(64)
        write_byte(p, 0, 7)
        i = i + 1
    end
    print(i)
end.
