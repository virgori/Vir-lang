func test_amx_matmul():
    let a = alloc(64)
    let b = alloc(64)
    write_word(a, 0, 42)
    write_word(b, 0, 100)

    # In Vir, a ** b is the native tensor/matrix multiplication operator
    let c = a ** b
    out c
end.

func main:
    let r = test_amx_matmul()
    out 0
end.
