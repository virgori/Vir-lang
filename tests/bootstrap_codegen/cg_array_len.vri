# cg_array_len.vri — Test builtin len and UFCS len on arrays
func main:
    let a = [10, 20, 30, 40, 50, 60, 70]
    print(len(a))
    print(a.len())
    let b = [42; 50]
    print(len(b))
    print(b.len())
    out 0
end.
