# EXPECT: AlphaBetaGamma\n14
func main:
    let s1 = "Alpha"
    let s2 = "Beta"
    let s3 = "Gamma"
    let full = str_concat(str_concat(s1, s2), s3)
    print_str(full)
    let len = str_len(full)
    print(len)
    out 0
end.
