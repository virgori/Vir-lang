func parse_print_stmt:
    in(p: i64)
    line = 0
    n = 0
    var (p2, val) = parse_expr(p)
    n = val
    out (p2, n)
end.

func parse_expr:
    in(x: i64)
    out (x, x)
end.

func main:
    var (a, b) = parse_print_stmt(5)
    print a
end.
