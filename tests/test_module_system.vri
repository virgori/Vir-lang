# Test: Module system features (§3)
# Tests: include, include as, import, get, share, port, lazy include, export, module
# expect: 100

# Module declarations (compile-time only, parser must accept)
include types;
include vir::rt::syscall;
include net::http as web;
lazy include circular_dep;

import Vec from vec;
import str_new, str_len from string;
from string import str_concat;
get PI from math;

export my_func;
share counter;
module myapp;
port events: int;

func main:
    print(100)
end.
