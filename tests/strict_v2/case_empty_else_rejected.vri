# The default arm must contain at least one statement.
# EXPECT_DIAGNOSTIC_MEMBER: case else arm must contain at least one statement
func main:
    case 1
        1: print(1)
        else
    end
end.
