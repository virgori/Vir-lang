# §13.7: compiler must issue W302 for retrying with dirty non-isolated state.
func main:
    var attempts = 0
    try:
        attempts = attempts + 1
        throw 1
    revert
        if attempts < 2 do
            resume retry
        end
    end
end.
