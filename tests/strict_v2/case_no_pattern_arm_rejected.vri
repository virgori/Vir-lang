# `case` requires at least one pattern arm; an `else` alone is insufficient.
# EXPECT_DIAGNOSTIC_MEMBER: case requires at least one pattern arm
func main:
    case 1
        else print(0)
    end
end.
