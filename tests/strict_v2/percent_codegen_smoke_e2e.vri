# Backend smoke: lowering a postfix percent must not fall back to `mod` or an
# unsupported intrinsic. Detailed floating-point comparisons live in the main
# percentage E2E tests.
func main() -> int:
    var value = 25%
    out 0
end.
