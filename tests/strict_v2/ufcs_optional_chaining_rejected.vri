# EXPECT_DIAGNOSTIC_LINE: 11
# EXPECT_DIAGNOSTIC_MEMBER: safe navigation method call
# UFCS: Optional chaining method call ?.() is not supported and must be rejected.

entity Item:
    val: int
end.

func main:
    var it = Item(val: 1)
    it?.score()
    out 0
end.
