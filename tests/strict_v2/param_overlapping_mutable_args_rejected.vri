# EXPECT_DIAGNOSTIC: E5003
# §14 borrow edge: two simultaneous mutable aliases are rejected.

func swap(ref left: int, ref right: int):
    var temporary = left
    left = right
    right = temporary
end.

func main:
    var value = 10
    swap(value, value)
    out 0
end.
