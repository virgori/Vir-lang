# `:` belongs after each pattern, not after the case expression.
# EXPECT_DIAGNOSTIC_MEMBER: case expression does not take ':'; ':' belongs after each pattern
func main:
    case 1:
        1: print(1)
    end
end.
