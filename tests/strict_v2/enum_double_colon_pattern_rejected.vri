# EXPECT_DIAGNOSTIC_MEMBER: invalid token '::'; use '.' for qualified names

enum Option<T>:
    Some(value: T)
    None
end.

func main:
    let value = Option.Some(1)
    case value
        Option::Some(inner): print(inner)
        Option.None: print(0)
    end
    out 0
end.
