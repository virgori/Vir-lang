# EXPECT:
extern from os func getuid() -> int

func main -> int:
    let pid = getpid()
    let uid = getuid()
    if pid > 0 do out 71 end
    if uid < 0 do out 22 end
    print "PASS: ffi extern getpid and getuid verified"
    out 0
end.
