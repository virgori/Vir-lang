# A move in either branch is visible after an if-statement join.
func consume(value):
    out 0
end.

func main:
    var values = [1, 2, 3]
    var cond = 1
    if cond == 1 do
        consume(values)
    end
    print(values) # E5001
    out 0
end.
