module test_mul_op
include alloc; import vir_alloc from alloc
include io; import print_ln from io

func main:
    let buf = vir_alloc(10)
    extern func native_write_u8(addr: int, offset: int, val: int)

    let a1 = 51
    let b1 = 10
    let c1 = a1 * b1
    native_write_u8(buf, 0, 0)
    out 0
end
