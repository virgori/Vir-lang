# Parser/lowering smoke test for target-neutral FFI declarations.
extern from os func getpid() -> int
extern from "/usr/lib/libSystem.B.dylib" func getuid() -> int

func main:
    let pid = getpid()
    if pid > 0 do
        print "PASS: ffi extern getpid verified"
    end
end.
