# EXPECT_COMPILE_FAIL: E5001
# A by-value call consumes an affine array argument exactly once.
func consume(value):
    out 0
end.

func main:
    var values = [1, 2, 3]
    consume(values)
    consume(values) # E5001
    out 0
end.
