# §4.8 — Pass-by-value of move-typed arg consumes caller's binding
func take:
    in(x)
    out x[0]
end.
func main:
    var a = [1, 2, 3]
    var r = take(a)      # 'a' is moved into take()
    print r
    print a[0]           # ERROR: use of moved value 'a'
end.
