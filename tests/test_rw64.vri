# EXPECT_START
# 42
# 99
# EXPECT_END
include alloc
import alloc from alloc

func main:
    var p = alloc(16)
    native_write_i64(p as int, 0, 42)
    native_write_i64(p as int, 8, 99)
    print(native_read_i64(p as int, 0))
    print(native_read_i64(p as int, 8))
    out 0
end.
